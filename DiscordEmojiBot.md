# Discord 개인 이모티콘 검색·추천 Bot/App 개발 프로젝트

## 1. 프로젝트 목적

Discord에서 사용할 개인용 이모티콘 검색·추천 시스템을 개발한다.

사용자는 Discord의 일반 1:1 Direct Message, Group DM, 서버 채팅방에서 대화할 때 등록해 둔 이모티콘을 빠르게 검색하고 선택하여 사용할 수 있어야 한다.

Discord 공식 Bot/App 기능을 기반으로 구현하며, 비공식 Discord 클라이언트 수정이나 사용자 토큰을 이용한 자동화 방식은 사용하지 않는다.

핵심 목표는 다음과 같다.

1. 이모티콘을 URL만으로 등록
2. URL에서 이모티콘 이미지 자동 추출
3. 모든 이미지를 동일한 규격의 투명 PNG로 관리
4. 각 이모티콘에 이름/키워드/감정 태그를 부여
5. 이모티콘 이름 또는 `#감정`으로 검색
6. 사용자의 현재 감정에 맞는 이모티콘 추천
7. 검색 결과에서 여러 이모티콘을 확인하고 하나를 선택
8. 선택한 이모티콘을 Discord 대화에서 사용할 수 있도록 제공
9. 향후 AI 기반 감정 분석 및 자동 추천 기능으로 확장할 수 있는 구조로 개발

---

# 2. 사용 환경

주 사용 환경은 Discord이다.

지원해야 하는 대화 유형:

* 1:1 Direct Message
* Group DM
* Discord Server의 일반 채팅방
* Discord Server의 기타 메시지 채널

Discord 공식 API 및 Bot/App 기능을 우선 사용한다.

사용자 토큰을 직접 사용하는 방식이나 Discord 클라이언트를 비공식적으로 수정하는 방식은 사용하지 않는다.

---

# 3. Discord 명령어

이모티콘 검색의 기본 Slash Command는 **`/e`**를 사용한다.

최대한 짧고 빠르게 이모티콘을 검색하고 사용할 수 있도록 설계한다.

기본 사용 방식:

```text
/e
```

→ 이모티콘 검색 UI 표시

```text
/e 아무튼
```

→ `아무튼`과 관련된 이모티콘 검색

```text
/e #황당
```

→ `#황당` 감정 태그가 연결된 이모티콘 검색

```text
/e #귀찮음
```

→ `#귀찮음` 감정 태그가 연결된 이모티콘 검색

가능하다면 Discord Slash Command의 Autocomplete 기능을 적극적으로 활용하여 사용자가 `/e` 입력 후 검색어를 빠르게 선택할 수 있도록 한다.

---

# 4. 이모티콘 등록

사용자가 직접 이미지 파일을 업로드하는 방식이 아니라 URL을 입력하여 등록한다.

예시:

`https://e.kakao.com/t/noorung-anyway-3?t_obj=creator_item`

사용자가 URL을 입력하면 서버가 해당 페이지를 분석하여 이모티콘 이미지를 추출한다.

## 등록 처리 과정

```text
URL 입력
 ↓
URL 접근
 ↓
페이지 분석
 ↓
이모티콘 이미지 추출
 ↓
이미지 다운로드
 ↓
PNG 변환
 ↓
배경 투명도 확인
 ↓
동일한 규격으로 리사이즈
 ↓
저장
 ↓
이모티콘 메타데이터 등록
```

특정 사이트의 HTML 구조에 지나치게 종속되지 않도록 URL 추출 로직은 Adapter/Parser 구조로 설계한다.

예:

```text
KakaoParser
GenericImageParser
FutureParser...
```

사이트 구조가 변경되었을 때 전체 시스템을 수정하지 않고 해당 Parser만 수정할 수 있도록 한다.

---

# 5. 이미지 규격

등록되는 모든 이모티콘은 동일한 이미지 규격으로 관리한다.

기본 저장 포맷:

* PNG
* 투명 배경
* 동일한 가로/세로 크기
* 이미지 비율 유지
* 원본 이미지가 정사각형이 아니더라도 왜곡하지 않음
* 필요하면 투명 영역을 추가하여 정사각형 Canvas에 배치

권장 기본 크기는 512x512로 설계하되, Discord에서 실제 사용하는 크기와 용량 제한을 고려하여 최종 구현 단계에서 적절한 규격을 결정한다.

원본 이미지와 가공 이미지는 필요하면 분리해서 보존한다.

```text
original/
processed/
```

---

# 6. 이모티콘 데이터 구조

이모티콘은 이미지와 메타데이터를 분리해서 관리한다.

기본적으로 다음 정보를 저장한다.

```text
id
name
source_url
original_image_path
processed_image_path
width
height
created_at
updated_at
```

검색을 위해 키워드와 감정을 별도로 관리한다.

예:

```text
name:
누룽이_아무튼

keywords:
아무튼
누룽이
어이없음
황당
체념

emotions:
#어이없음
#황당
#체념
#무덤덤
#귀찮음
```

---

# 7. 감정 태그 시스템

이 프로젝트에서 감정 태그는 핵심 기능이다.

각 이모티콘에는 하나 이상의 감정 태그를 붙일 수 있어야 한다.

예:

```text
#기쁨
#슬픔
#분노
#황당
#어이없음
#체념
#귀찮음
#무덤덤
#놀람
#당황
#감동
#축하
#동의
#거절
#의문
#생각
```

하나의 이모티콘이 여러 감정에 동시에 속할 수 있어야 한다.

예:

```text
누룽이_아무튼

#어이없음
#황당
#체념
#무덤덤
```

따라서 DB 구조는 이모티콘과 감정을 N:M 관계로 설계한다.

---

# 8. 검색 기능

검색은 최소한 다음 세 가지를 지원한다.

## 8-1. 이모티콘 이름 검색

```text
/e 아무튼
```

검색 결과:

```text
누룽이_아무튼
아무튼_그래
아무튼_모르겠다
...
```

---

## 8-2. 감정 태그 검색

```text
/e #황당
```

결과:

```text
#황당
#어이없음
#체념
```

등의 감정과 연결된 이모티콘을 출력한다.

---

## 8-3. 키워드 검색

```text
/e 귀찮음
```

또는

```text
/e 피곤
```

처럼 검색하면 해당 키워드와 연결된 이모티콘을 출력한다.

---

# 9. Discord 검색 UI

`/e` 명령어를 실행하면 가능한 한 Discord의 공식 Interaction UI를 활용하여 검색 화면을 제공한다.

예:

```text
/e
```

↓

```text
┌──────────────────────────────────────┐
│ 🔎 이모티콘 검색                      │
├──────────────────────────────────────┤
│                                      │
│ 검색어를 입력하세요                   │
│                                      │
├──────────────────────────────────────┤
│ 최근 사용                             │
│                                      │
│ [🖼] [🖼] [🖼] [🖼]                   │
│                                      │
└──────────────────────────────────────┘
```

검색:

```text
/e 아무튼
```

↓

```text
┌──────────────────────────────────────┐
│ 검색 결과: 아무튼                     │
├──────────────────────────────────────┤
│                                      │
│ [이미지]  [이미지]  [이미지]          │
│ 아무튼1   아무튼2   아무튼3           │
│                                      │
│ [이미지]  [이미지]  [이미지]          │
│ 아무튼4   아무튼5   아무튼6           │
│                                      │
├──────────────────────────────────────┤
│ #황당 #체념 #무덤덤                   │
└──────────────────────────────────────┘
```

사용자가 원하는 이모티콘을 선택할 수 있어야 한다.

---

# 10. 감정 기반 추천

단순 검색뿐만 아니라 사용자의 현재 감정과 비슷한 이모티콘을 검색할 수 있어야 한다.

1차 버전에서는 사용자가 감정을 직접 선택한다.

예:

```text
현재 감정

[😀 기쁨]
[😐 무덤덤]
[😑 귀찮음]
[😡 분노]
[😭 슬픔]
[🤔 생각]
[😳 당황]
[😂 웃김]
```

예를 들어 사용자가 `😑 무덤덤`을 선택하면:

```text
#무덤덤
#귀찮음
#체념
#어이없음
```

등 관련 감정 태그를 기반으로 이모티콘을 추천한다.

감정 간 유사도 개념을 도입할 수 있도록 설계한다.

예:

```text
무덤덤
 ├── 귀찮음
 ├── 체념
 └── 어이없음
```

단, 처음부터 복잡한 AI 모델을 강제하지 않고 DB 기반 태그 매칭으로 구현한다.

---

# 11. 향후 AI 감정 분석 확장

향후에는 사용자가 입력하려는 문장을 분석하여 자동으로 감정을 추정하고 이모티콘을 추천할 수 있어야 한다.

예:

```text
"아 진짜 이걸 지금 또 해야 하네..."
```

↓

```text
감정 분석

#귀찮음
#어이없음
#체념
```

↓

```text
추천 이모티콘

[이미지] [이미지] [이미지]
[이미지] [이미지] [이미지]
```

따라서 현재 설계 단계부터 AI 감정 분석 기능을 추가하기 쉬운 구조로 만든다.

AI 기능은 1차 구현 범위에서 제외해도 되지만 API/Service 계층을 확장할 수 있도록 설계한다.

---

# 12. 이모티콘 선택 및 Discord 사용

검색 결과에는 여러 이모티콘을 표시한다.

사용자가 하나를 선택하면 해당 이모티콘을 Discord 메시지에서 사용할 수 있어야 한다.

Discord API의 실제 제약을 먼저 확인하고, 가능한 공식 기능 범위 내에서 구현한다.

특히 다음을 명확하게 구분해서 설계한다.

```text
1. Discord Custom Emoji로 등록하는 방식
2. 메시지에 이미지 파일/첨부파일로 보내는 방식
3. 링크를 전송하는 방식
4. Discord Interaction에서 선택 후 메시지로 전송하는 방식
```

사용자가 원하는 UX를 가장 가깝게 구현하되, Discord API에서 지원하지 않는 기능은 억지로 우회하지 않는다.

---

# 13. Discord Custom Emoji 고려

가능하다면 등록된 PNG를 Discord 서버의 Custom Emoji로 자동 등록하는 방법을 검토한다.

다만 Discord의 서버별 Emoji 개수, 파일 크기, 권한 및 API 제한을 확인한다.

이모티콘 데이터베이스와 Discord Custom Emoji ID를 연결할 수 있도록 한다.

예:

```text
emoticon_id
discord_guild_id
discord_emoji_id
```

하나의 이모티콘을 여러 Discord 서버에서 사용할 수 있는 구조를 고려한다.

---

# 14. 개인 DM 사용에 대한 설계

이 프로젝트는 서버 채팅뿐 아니라 1:1 DM과 Group DM에서도 사용하려는 목적이 있다.

따라서 Discord의 공식 API에서 DM/Group DM에서 Bot/App이 어느 범위까지 동작할 수 있는지 먼저 조사한다.

지원되지 않는 기능은 명확하게 구분하고 대체 UX를 설계한다.

특히 다음을 확인한다.

* Bot이 일반 DM에서 Slash Command를 사용할 수 있는가
* Group DM에서 가능한가
* Discord App Command가 DM에서 어떤 범위로 동작하는가
* Custom Emoji가 DM에서 어떤 방식으로 표시되는가
* Bot이 사용자 대신 일반 메시지를 작성하는 것이 가능한가
* Interaction Response로 이미지를 보내는 것이 가능한가

이 부분은 구현 전에 반드시 최신 Discord 공식 문서를 기준으로 검증한다.

---

# 15. 관리자 기능

이모티콘을 관리할 수 있는 관리자 기능을 만든다.

최소 기능:

```text
이모티콘 목록
이모티콘 검색
이모티콘 등록
URL 등록
이미지 미리보기
이름 수정
키워드 수정
감정 태그 추가/삭제
이모티콘 삭제
중복 검사
```

등록 화면 예:

```text
URL:
[ https://e.kakao.com/... ]

[이미지 추출]

추출 결과:

[이미지] [이미지] [이미지]
[이미지] [이미지] [이미지]

이름:
[누룽이 아무튼]

감정:
[#어이없음] [#황당] [#체념]

키워드:
[아무튼] [누룽이] [귀찮음]

[등록]
```

---

# 16. 중복 처리

동일 URL을 다시 등록하는 경우 중복 등록하지 않는다.

가능하다면 이미지 Hash를 계산하여 동일한 이미지가 다른 URL에서 등록되는 경우도 감지한다.

다음 기준을 각각 관리한다.

```text
source_url 중복
image_hash 중복
```

---

# 17. 검색 결과 정렬

검색 결과는 다음 기준을 고려한다.

1. 정확히 일치하는 이름
2. 이름 부분 일치
3. 정확히 일치하는 감정 태그
4. 키워드 일치
5. 감정 유사도
6. 최근 사용
7. 사용 빈도

단, 초기 버전에서는 지나치게 복잡한 검색 엔진을 사용하지 말고 구현 및 유지보수가 쉬운 구조로 만든다.

---

# 18. 최근 사용 / 즐겨찾기

향후 사용성을 고려하여 다음 기능을 확장할 수 있도록 한다.

```text
최근 사용
즐겨찾기
사용 빈도
개인별 추천
```

예:

```text
/e

최근 사용
[🖼] [🖼] [🖼] [🖼]

즐겨찾기
[🖼] [🖼] [🖼] [🖼]
```

---

# 19. 기술 스택

기술 스택은 프로젝트 규모와 유지보수성을 고려하여 제안하되 다음 조건을 우선 검토한다.

Backend:

* Python 우선
* FastAPI 또는 적절한 Python Web Framework
* Discord 공식 라이브러리 사용

Database:

* 개발 단계: SQLite 가능
* 운영 환경: PostgreSQL 확장 가능하도록 설계
* ORM/Repository 구조 분리

Image Processing:

* Pillow 등 검증된 이미지 처리 라이브러리

Frontend:

* Discord Interaction UI 우선
* 필요한 경우 별도 관리자 Web UI 추가

Storage:

* 개발 단계: 로컬 파일 저장
* 향후 S3 호환 Object Storage로 변경 가능하도록 Storage abstraction 적용

---

# 20. 관리자 기능

관리자는 별도의 Web UI에서 이모티콘을 관리할 수 있도록 한다.

최소 기능:

```text
이모티콘 목록
이모티콘 검색
이모티콘 등록
URL 등록
이미지 미리보기
이름 수정
키워드 수정
감정 태그 추가/삭제
이모티콘 삭제
중복 검사
```

---

# 21. 보안 및 운영

다음 사항을 반드시 고려한다.

* Discord Bot Token은 환경변수로 관리
* `.env` 파일을 Git에 커밋하지 않음
* 관리자 기능은 인증 필요
* URL 다운로드 시 SSRF 방어
* 허용되지 않은 내부 IP 접근 차단
* 이미지 파일 확장자 위조 방지
* 이미지 크기 제한
* 다운로드 타임아웃
* 악성 이미지 방어
* HTML/JS 파일을 이미지로 위장한 경우 방어
* 외부 사이트 요청 rate limit
* Discord API rate limit 준수
* 사용자 입력 sanitizing
* 로그에 Bot Token 등 민감정보 출력 금지

---

# 22. 프로젝트 구조

처음부터 유지보수하기 좋은 구조로 구성한다.

예시:

```text
discord-emoticon-bot/
│
├── app/
│   ├── main.py
│   │
│   ├── discord/
│   │   ├── bot.py
│   │   ├── commands/
│   │   ├── interactions/
│   │   └── views/
│   │
│   ├── emoticon/
│   │   ├── service.py
│   │   ├── repository.py
│   │   ├── models.py
│   │   └── schemas.py
│   │
│   ├── crawler/
│   │   ├── base.py
│   │   ├── kakao.py
│   │   └── generic.py
│   │
│   ├── image/
│   │   ├── processor.py
│   │   └── validator.py
│   │
│   ├── emotion/
│   │   ├── service.py
│   │   └── tags.py
│   │
│   ├── search/
│   │   └── service.py
│   │
│   └── storage/
│       ├── base.py
│       └── local.py
│
├── tests/
├── migrations/
├── data/
├── .env.example
├── requirements.txt
├── Dockerfile
├── docker-compose.yml
└── README.md
```

실제 프로젝트에 더 적합한 구조가 있다면 개선안을 먼저 제시한다.

---

# 23. 개발 단계

한 번에 모든 기능을 구현하지 말고 단계적으로 개발한다.

## Phase 1 — Discord Bot/App 기본 구축

* Bot 생성
* Discord 연결
* `/e` Slash Command
* 기본 Interaction
* 기본 응답
* Autocomplete 검토

## Phase 2 — 이모티콘 등록

* URL 입력
* 이미지 추출
* PNG 변환
* 이미지 표준화
* 저장

## Phase 3 — DB 구축

* 이모티콘
* 키워드
* 감정
* 관계 테이블

## Phase 4 — 검색

* 이름 검색
* 키워드 검색
* `#감정` 검색
* `/e` Autocomplete

## Phase 5 — Discord UI

* 검색 입력
* 검색 결과
* 여러 이미지 표시
* 선택 기능

## Phase 6 — 감정 기반 추천

* 감정 선택
* 관련 태그 검색
* 유사 감정 추천

## Phase 7 — 관리자 Web UI

* 등록
* 수정
* 삭제
* 태그 관리
* 이미지 관리

## Phase 8 — 고도화

* 최근 사용
* 즐겨찾기
* 사용 빈도
* AI 감정 분석
* 추천 알고리즘
* 다중 Discord 서버 지원
* Object Storage
* PostgreSQL

---

# 24. 반드시 먼저 해야 할 작업

코드를 바로 작성하지 말고 다음 작업을 먼저 수행한다.

## 1. Discord 공식 API의 현재 기능 조사

특히 다음을 최신 공식 문서 기준으로 확인한다.

* Slash Commands
* Autocomplete
* Context Menu
* Modals
* Buttons
* Select Menus
* Ephemeral Response
* Attachment
* Custom Emoji
* DM
* Group DM
* Server Channel

## 2. 원하는 UX의 구현 가능 여부 확인

다음 기능을 각각 검증한다.

```text
A. /e 검색
B. 검색 결과 여러 개 표시
C. 이미지 선택
D. 선택한 이미지의 Discord 메시지 전송
E. 서버 Custom Emoji 등록
F. DM에서 사용
G. Group DM에서 사용
```

각 기능을 다음 세 가지로 구분한다.

```text
가능
조건부 가능
불가능
```

그리고 조건이 있다면 정확히 설명한다.

## 3. Discord에서 불가능한 기능은 임의로 우회하지 않는다.

Discord 공식 기능 범위 내에서 가장 유사한 UX를 제안한다.

---

# 25. 핵심 UX

최종적으로 사용자가 원하는 흐름은 다음과 같다.

```text
Discord 채팅 중

/e
 ↓
이모티콘 검색 UI
 ↓
"아무튼" 입력
 ↓

┌─────────────────────────────┐
│ 검색 결과                   │
│                             │
│ 🖼️   🖼️   🖼️   🖼️          │
│                             │
│ 🖼️   🖼️   🖼️   🖼️          │
└─────────────────────────────┘

 ↓
원하는 이모티콘 선택
 ↓
Discord에서 해당 이모티콘 사용
```

또는:

```text
/e #황당
```

↓

```text
#황당과 관련된 이모티콘 목록
```

또는:

```text
/e --emotion
```

↓

```text
현재 감정 선택

😐 무덤덤
😡 분노
😂 웃김
😭 슬픔
🤔 생각
...
```

↓

```text
관련 이모티콘 추천
```

최종 목표는 **Discord에서 개인 이모티콘을 이름/키워드/감정으로 빠르게 검색하고 선택해서 사용하는 개인용 Discord App**이다.

---

# 26. 코드 작성 원칙

* 실제 실행 가능한 코드를 작성한다.
* 단순 예제 코드가 아니라 실제 프로젝트 구조로 만든다.
* 핵심 기능을 TODO로 남기지 않는다.
* Discord API의 실제 제약을 확인한다.
* 환경변수로 설정을 분리한다.
* 예외 처리를 포함한다.
* Logging을 포함한다.
* Type Hint를 적극적으로 사용한다.
* 함수/클래스 책임을 명확하게 분리한다.
* 테스트 가능한 구조로 만든다.
* 외부 URL Parser를 Adapter 구조로 만든다.
* 이미지 처리와 Discord 처리를 강하게 결합하지 않는다.
* DB Repository와 Business Logic을 분리한다.

---

# 27. 최종 결과물

최종적으로 다음을 제공한다.

1. 전체 프로젝트 소스 코드
2. requirements.txt 또는 pyproject.toml
3. `.env.example`
4. DB 초기화 및 Migration
5. Dockerfile
6. docker-compose.yml
7. Discord Bot 설치 방법
8. Discord 권한 설정 방법
9. 개발 환경 실행 방법
10. 운영 환경 실행 방법
11. 관리자 사용 방법
12. 이모티콘 URL 등록 방법
13. `/e` 사용 방법
14. 검색/감정 추천 사용 방법
15. 테스트 코드
16. README.md

---

# 28. 개발 진행 방식

먼저 코드를 무작정 작성하지 말고 다음 순서로 진행한다.

```text
1. 요구사항 분석
2. Discord API 최신 제약사항 확인
3. 가능한 UX 정의
4. 전체 Architecture 설계
5. DB Schema 설계
6. Discord Interaction 설계
7. 프로젝트 구조 제안
8. 구현
9. 테스트
10. 실행 방법 작성
```

특히 **Discord 공식 API에서 불가능한 기능을 가능한 것처럼 가정하지 않는 것**을 가장 중요하게 한다.

현재 시점의 Discord 공식 문서를 기준으로 기능 가능 여부를 확인하고, 구현 전에 그 결과를 먼저 정리한 후 실제 코드를 작성한다.

최종 목표는 **`/e` 하나를 중심으로 개인 이모티콘을 검색하고, 감정 기반으로 추천받고, 선택하여 Discord에서 사용할 수 있는 개인용 이모티콘 App**이다.
